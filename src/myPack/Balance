package MyPack;
class Balance {
  String name;
  float bal;
  
  Balance (String name, float bal) {
    this.name - name;
    this.bal - bal;
  }
  
  void show() {
    if (bal < 0) {
      System.out.println("-->");  
    }
    System.out.println (name + ": $" + bal);
  }
}
